# For integration tests only
version: "3.6"

services:
  mongo:
    image: mongo:5@sha256:82a55eb6d60997007ff390087d4e064218d477e9611a7becd78664a2ab490eff

  scraper:
    build:
      context: .
    depends_on:
      - mongo
    environment:
      MONGODB_CONNECTION_STRING: mongodb://mongo:27017/ssu-criminals
