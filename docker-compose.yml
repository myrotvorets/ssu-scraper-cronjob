# For integration tests only
version: "3.6"

services:
  mongo:
    image: mongo:5@sha256:0305e68a63b620995d239ad72aac7c15df4d5dab3c81b18c438364ced291cf82

  scraper:
    build:
      context: .
    depends_on:
      - mongo
    environment:
      MONGODB_CONNECTION_STRING: mongodb://mongo:27017/ssu-criminals
